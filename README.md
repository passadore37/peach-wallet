# criar app Next.js (aceite opções padrão)
npx create-next-app@latest peach-wallet
cd peach-wallet

# inicializar repositório local (se ainda não estiver)
git init
git add .
git commit -m "Initial Next.js app"
